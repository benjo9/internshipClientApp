<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.dailab.androlyzer.client"
    android:versionCode="2"
    android:versionName="1.1" >

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="15" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".ui.ActivityHome"
            android:label="@string/app_name"
            android:theme="@style/Theme.Androlyzer" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.ActivityAppDetail"
            android:label="@string/label_app_detail"
            android:theme="@style/Theme.Androlyzer" >
        </activity>
        <activity
            android:name=".ui.ActivityPreference"
            android:label="@string/label_preference"
            android:theme="@style/Theme.Androlyzer" >
        </activity>
    </application>

</manifest>
